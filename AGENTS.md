# 个人记忆 Wiki：Codex 工作规范

本 Vault 使用 LLM Wiki 方法论构建个人记忆系统。开始任何 Wiki 工作前，先阅读 `purpose.md` 和 `schema.md`。

## 会话启动与记忆加载

每次在本仓库中新开 AI 会话时，在处理用户的第一个实质请求前：

1. 自动读取 `purpose.md`、`schema.md`、`wiki/index.md` 和 `wiki/overview.md`，建立个人记忆库的基础上下文。
2. 不在启动阶段遍历整个 `wiki/` 或 `obsidian/`；根据用户问题继续读取相关 Wiki 页面。
3. 涉及用户的知识、经历、项目、决定、兴趣或方向时，优先从 `wiki/` 检索；需要精确细节或证据时，再沿双链读取 `obsidian/` 原文。
4. 用户不需要重复说明“加载记忆库”。只要会话工作目录位于本仓库，就将记忆库视为默认可用上下文。
5. 加载记忆只代表读取，不代表修改。只有用户要求“同步记忆库”或表达同等意图时，才执行下方同步工作流。
6. 如果入口文件缺失或无法读取，应明确报告；不得假装已经加载记忆库。

## 内容所有权

- `wiki/` 是 Codex 可以创建和增量维护的派生记忆层。
- `obsidian/` 中的用户笔记与附件属于人类原始记忆层。
- 未经用户明确要求，不得修改、移动、重命名或删除任何原始笔记和附件。
- 不得为了统一结构而移动现有目录、集中图片或改写原文。
- 忽略 `schema.md` 中列出的工具、配置和系统文件。

## “同步记忆库”工作流

当用户要求“同步记忆库”“更新 Wiki”“摄入这些笔记”或表达同等意图时：

1. 在 `obsidian/` 中确定本次新增或发生变化的原始笔记；范围不明确时先报告拟处理范围。
2. 阅读 `purpose.md`、`schema.md`、`wiki/index.md` 和相关 Wiki 页面。
3. 搜索现有 Wiki，优先更新已有页面，避免同义页面和重复节点。
4. 提取值得长期保留的概念、实体、项目、决定和跨笔记模式。
5. 使用 Vault 相对路径的 Obsidian `[[双链]]` 标注原始来源。
6. 区分原文事实、用户观点和 AI 推断；证据不足时写“待核实”。
7. 只更新受影响的 Wiki 页面，并同步维护 `wiki/index.md` 与 `wiki/overview.md`。
8. 在 `wiki/log.md` 末尾追加本次处理记录，包括日期、来源、新建页面、更新页面和待人工确认事项。
9. 完成后自检 YAML、双链、重复页面和未注明来源的结论，再向用户汇报变更。

## 查询工作流

当用户基于个人记忆提问时：

1. 先从 `wiki/index.md` 和相关 Wiki 页面检索。
2. 需要核验时沿 `sources` 或正文双链回到原始笔记。
3. 回答中引用相关 Wiki 页面和原始笔记路径。
4. 如果本次查询形成了值得长期保存的新综合，先提出写入建议；除非用户已要求保存，否则不自动写入。

## Lint 工作流

当用户要求检查 Wiki 健康度时，检查：

- 没有入链或出链的孤立页面
- 缺少原始来源的结论
- 同义、重复或互相矛盾的页面
- 新来源已经使旧结论过时的内容
- 长期未更新的项目或决定
- 被多次提到但尚未建立页面的重要概念
- 失效的 Obsidian 双链和无效 YAML

## 安全规则

- 不将 Wiki 中的 AI 推断当作原始事实。
- 不自动删除内容；需要清理时先列出候选项并等待用户确认。
- 不把私人笔记发送到外部服务，除非用户明确授权具体内容和目的地。
- 不覆盖 `status: reviewed` 页面中的人工内容；需要调整时保留原文并说明依据。
