---
title: 检索、RAG 与知识系统
part: 2
parent: "[[大纲 - Agent 从 0 到 1 开发大纲]]"
status: drafting
---

# 检索、RAG 与知识系统

这一部分想解决的问题是：模型很强，但模型参数里的知识，并不自动等于“我这次任务真正需要的知识”。

当任务开始依赖本地文件、私有资料、最新网页、团队文档、可追溯证据时，只靠参数知识就不够了。于是我们才会引入检索、RAG，以及更完整的知识系统。

## 阅读顺序

| 章节 | 主题 | 你会看懂什么 | 状态 |
| --- | --- | --- | --- |
| [[02.01-为什么需要外部知识]] | 知识缺口从哪里来 | 参数知识、上下文知识、检索知识、记忆分别是什么 | 初稿 |
| [[02.02-从 File Search 开始]] | 最小检索能力 | 文件名、全文、正则、语义检索分别适合什么问题 | 待写 |
| [[02.03-基础 RAG 是怎么工作的]] | 标准 RAG 链路 | 切分、Embedding、召回、Rerank、生成与引用 | 待写 |
| [[02.04-Agentic Search]] | 让 Agent 自己规划搜索 | 查询改写、子问题拆分、数据源选择与停止条件 | 待写 |
| [[02.05-Agentic RAG]] | 把检索真正接进 Agent Loop | 检索作为工具、多跳检索、证据回传与继续决策 | 待写 |
| [[02.06-知识系统的边界]] | 概念辨析 | Knowledge Base、LLM Wiki、Memory、Context 的区别 | 待写 |
| [[02.07-怎么评测检索效果]] | 不是“能跑就算成功” | Recall、Top-k、引用质量、事实一致性与失败分类 | 待写 |

## 本部分主线

你可以先把这一部分理解成一条逐层补知识的链路：

1. 模型参数里有很多常识和通用知识，但不一定新，也不一定刚好是你需要的。
2. 如果把所有资料都硬塞进 prompt，会很贵、很慢，而且容易把模型喂晕。
3. 所以我们需要先检索，再只把真正相关的材料喂给模型。
4. 这就是 RAG 的基本出发点：先找，再答。
5. 当检索本身也需要规划、分解和多轮判断时，就会演化成 Agentic Search 和 Agentic RAG。
6. 再往上走，就不只是“搜文档”，而是开始建设一个长期可维护、可更新、可追溯的知识系统。

## 这一部分最想讲清什么

| 问题 | 结论方向 |
| --- | --- |
| 模型不是已经知道很多东西了吗？ | 知道很多，不等于知道这次任务需要的最新、私有、可验证知识。 |
| 为什么不能直接把所有资料都塞进上下文？ | 因为上下文窗口、成本、延迟和噪音都会成为问题。 |
| RAG 的本质是什么？ | 不是“用了向量数据库”，而是“先检索相关证据，再基于证据生成回答”。 |
| 检索系统和知识系统一样吗？ | 不一样。检索系统解决“找”，知识系统还要解决结构、更新、权限、追溯和维护。 |
| Agent 和 RAG 的关系是什么？ | RAG 可以独立存在；Agent 则让检索变成可规划、可循环、可判断证据充分性的能力。 |

详细章节安排见[[大纲 - Agent 从 0 到 1 开发大纲#Part 02：检索、RAG 与知识系统|总大纲]]。
